<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MediaTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:shadowColor">@color/lightTextColor</item>
        <item name="android:shadowDx">@dimen/textViewShadowDx</item>
        <item name="android:shadowDy">@dimen/textViewShadowDy</item>
        <item name="android:shadowRadius">@dimen/textViewShadowRadius</item>
        <item name="android:textColor">@color/lightTextColor</item>
    </style>

</resources>
